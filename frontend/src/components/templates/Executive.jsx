import React from 'react';
import Minimal from './Minimal';

export default function Executive({ data }) {
  return (
    <div className="resume-executive-wrap">
      <Minimal data={data} />
    </div>
  );
}
